rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Cho phép đọc/ghi trong môi trường phát triển (cần cập nhật lại khi lên production)
      allow read, write: if true; 
    }
  }
}
